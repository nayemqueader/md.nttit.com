package nayem.md.nttit.com;

public class Inheritence extends Const2 {
//child class is Inheritance and parent class is Const2 
	
	public static void main(String[] args) {
	/*Inheritance we can divide into 2 categories
	 * 1. sub class which is child class, the class that inherit from another 
	 * class
	 * 2. super class which is parent class, the class being inherited from.  
	 * 
	 * purpose 1. code reusability 2. i can inherit the code from other class
	 * and then can use.
	 * if we inherit a class that means we can inherit all the data
	 * and the method of the class. 
	 * 
	 *  we can make parent child relationship, and child could have
	 *  access parents data and method. 
	 *  
	 *  to inherit parents data and method we have to use "extends" key word
	 */
		
		Inheritence obj = new Inheritence ();
	obj.fol();

	


	}

	

}
