package nayem.md.nttit.com;

public class SelenioumWait {
/*Synchronization
 * 
 * wait are 2 types
 * 1.Soft wait
 *  
 * soft wait are 3 types 
 * a) Implicit wait (global wait) we use this wait in between browser and url
 * b) explicit wait (in between web element)
 * c)Fluent wait
 * Wait wait = new FluentWait<WebDriver>(driver)
.withTimeout(30, TimeUnit.SECONDS)
.pollingevery(3, TimeUnit.SECONDS)
.ignoring(NoSuchElementException.class);
 * 
 * 2. Hard wait
 * thread.sleep()
*/
	
	
}
